<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Warna judul untuk dark mode -->
    <color name="warnaJudul">#FFFFFF</color>

    <!-- Tema malam (Dark Mode) -->
    <style name="Base.Theme.Kampusku" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/warnaPrimaryDark</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:statusBarColor">@color/warnaPrimaryDark</item>
        <item name="android:windowBackground">@color/warnaBackgroundDark</item>
    </style>

    <style name="Theme.Kampusku" parent="Base.Theme.Kampusku" />
</resources>

